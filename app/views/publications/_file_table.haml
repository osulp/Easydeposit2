- if @publication.publication_files.count > 0
  %table.table
    %tbody
      - @publication.publication_files.each do |f|
        %tr
          %th{scope: 'row'}=f.blob.filename
          %td= link_to "Delete", publication_delete_file_path(@wos_record.uid, f.id), method: :delete, data: { confirm: "Are you sure you want to delete this file?" }
- else
  %h5 No files have been attached to this work.
